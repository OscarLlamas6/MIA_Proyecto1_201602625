
Recibir offset de la extendida y el size de la extendida

EBR = Leer el primer EBR

Bool = true

ValorNext = Next del EBR

while Bool {

si valorNext = -1{

    si ValorPrev = 0{ //esto significa que es el primer EBR de la extendida

        si EBr.size == 0{// esto significa que el primer EBr de la extendida no apunta a ninguna espacio (0 logicas)
             si (size de la extendida) - (size del EBR) >= (size de la logica nueva){
                setear al EBR el .Start = (startExtendida + sizeofEBR) 
                setear al EBR el .size = size de la logica nueva
                return true              
             }

        } else {
            si (size de la extendida) - (size del EBR+ EBr.size) >= (sizeofEBR + size de la logica nueva){
                escribir nuevoEBR en seef= startExtendida + sizeofEBR + EBR.size
                setear EBR.next = startExtendida + sizeofEBR + EBR.size
                setear atributos al nuevoEBR, incluido el next = -1 y el prev = inicio de la extendida
                return true              
             }

        }

       

    } else {

        si (size de la extendida) - (EBr.STart+EBR.Size) >= (sizeofEBR+SizeOfNuevaLogica){
            Escribir un nuevoEBR en Seek = EBr.Start + EBR.Size 
            Setear en el nuevoEBR el Start = (EBr.Start + EBR.Size+sizeofEBR)
            Setear en el EBRanterior el Next = EBr.Start + EBR.Size 
            Setear al nuevoEBR el next = -1 y el prev = (start del anterior) - sizeofEBR
            return true

        }

    }

} else{
    
    si EBR.Next - (EBR.Start + EBR.Size) >= (sizeofEBR+ SizeOfNuevaLogica){


    Guardar temporalmente el EBRNext
    Escribir un nuevoEBR en Seek = EBr.Start + EBR.Size 
    Setear en el nuevoEBR el Start = (EBr.Start + EBR.Size+sizeofEBR)
    Setear en el EBRanterior el Next = EBr.Start + EBR.Size 
    Setear al nuevoEBR el next = dato guardado temporalmente del EBRNext antiguo y el prev = (EBRanterior.start - sizeofEBR)
    Guardar temporalmente el (nuevoEBR.start-sizeofEBR)
    Leer el siguiente EBR en Seek = Dato guardado termporalmente del EBRNext
    setear el prev = (nuevoEBR.start-sizeofEBR)
    return true

    }
    

}

si el next del ebr != -1 {
leer otro EBR con el NExt del EBR actal
ValorNext = Next Del EBR leido
} else {
    bool = false
}


}

return false